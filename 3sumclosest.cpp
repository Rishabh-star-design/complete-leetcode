/*question mai index nhi manga hai so first sort the array then use the two-pointer technique*/
#include <bits/stdc++.h>
using namespace std;
class Solution
{
    public:
    int threeSumCloses(vector<int> &nums, int target){
    int n=nums.size();
    sort(nums.begin(),nums.end());
    int closestSum=INT_MAX;
    

}}